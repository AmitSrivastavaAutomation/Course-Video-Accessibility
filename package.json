{
  "name": "course-video-accessibility",
  "version": "1.0.0",
  "scripts": {
    "test": "npx playwright test",
    "test:aicerts": "npx playwright test tests/aicerts.spec.js",
    "report": "npx playwright show-report reports/html"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}